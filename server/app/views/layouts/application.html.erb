<!DOCTYPE html>
<html>
<head>
  <title>httpillage C&C</title>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= stylesheet_link_tag  'https://nvisium.com/stylesheets/global.css' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>

  <script src="//cdn.datatables.net/1.10.9/js/jquery.dataTables.min.js"></script>
  <link rel="stylesheet" href="//cdn.datatables.net/1.10.9/css/jquery.dataTables.min.css">
</head>
<body>
	<div class="container">
		<nav class="nav-menu row">
			<div class="col-lg-8">
        <%= image_tag 'nvisium_logo.png', {:class => 'logo', :alt => 'nVisium logo'} %>
  			<%= link_to 'Jobs', jobs_path, {:class => 'nav-link link-primary'} %>
  			<%= link_to 'Nodes', nodes_path, {:class => 'nav-link link-primary'} %>
  			<%= link_to 'Dictionaries', dictionaries_path, {:class => 'nav-link link-primary'} %>
        <%= link_to 'Settings', settings_path, {:class => 'nav-link link-primary'} %>

        <% if current_user.try(:admin?) %>
          <%= link_to 'Users', users_path, { :class => 'nav-link link-primary' } %>
        <% end %>


       </div>
			<div class="col-lg-4" style="text-align: right; padding-top: 4px;"><%= render "layouts/shared/user_menu" if current_user %></div>
		</nav>
		<span class="notice" style="padding: 0; margin: 0; border: none"><p class="notification-box"><%= notice %></p></span>
		<span class="alert" style="padding: 0; margin: 0; border: none"><p class="notification-box"><%= alert %></p></span>
		<%= yield %>
	</div>

</body>
</html>
